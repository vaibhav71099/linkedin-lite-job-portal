spring.application.name=jobportal
app.db.vendor=${DB_VENDOR:mysql}
app.db.host=${DB_HOST:${MYSQLHOST:localhost}}
app.db.port=${DB_PORT:${MYSQLPORT:3306}}
app.db.name=${DB_NAME:${MYSQLDATABASE:jobportal}}
app.db.params=${DB_PARAMS:?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}

spring.datasource.url=${SPRING_DATASOURCE_URL:${JDBC_DATABASE_URL:jdbc:${app.db.vendor}://${app.db.host}:${app.db.port}/${app.db.name}${app.db.params}}}
spring.datasource.username=${DB_USERNAME:${MYSQLUSER:root}}
spring.datasource.password=${DB_PASSWORD:${MYSQLPASSWORD:change-me}}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET:VGhpc0lzQVNlY3VyZUp3dFNlY3JldEtleUZvckRlbW9Qcm9qZWN0MTIzNDU2Nzg5MA==}
jwt.expiration=${JWT_EXPIRATION:86400000}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
app.otp.expiry-minutes=${OTP_EXPIRY_MINUTES:10}

spring.mail.host=${SMTP_HOST:}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${SMTP_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:10000}
app.mail.from=${SMTP_FROM:}

twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.from-number=${TWILIO_FROM_NUMBER:}
